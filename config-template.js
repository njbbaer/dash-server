/** Must rename file as config.js */

var config = {};

config.buttons = [
	{
		name: CUSTOM NAME,
		mac: MAC ADDRESS,
		action: ACTION NAME,
		parameters: [
			OPTIONAL PARAMETERS
		]
	},
]

module.exports = config;